<form method="post" action="{{ path('app_humidity_delete', {'id': humidity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ humidity.id) }}">
    <button class="btn">Delete</button>
</form>
